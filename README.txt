General Info ---------------------------------
run `code.cpp` by `Ctrl+Shift+b` at VSCode
reads from `input.txt`
stdout to `output.txt`

{
  "key": "ctrl+shift+b",
  "command": "workbench.action.tasks.build",
  "when": "taskCommandsRegistered"
}